<?php

return [

    /*
    | ═══════════════════════════════════════════════════════
    | پیش‌ران ذخیره‌سازی نشست
    | ═══════════════════════════════════════════════════════
    |
    | گزینه‌ها: database | redis | file
    | پیش‌فرض: database — روی هر cPanel بدون دردسر کار می‌کند
    */
    'driver' => env('SESSION_DRIVER', 'database'),

    /*
    | ═══════════════════════════════════════════════════════
    | طول عمر نشست (دقیقه)
    | ═══════════════════════════════════════════════════════
    */
    'lifetime' => (int) env('SESSION_LIFETIME', 120),

    /*
    | ═══════════════════════════════════════════════════════
    | پایان نشست با بستن مرورگر
    | false = نشست حتی بعد از بستن مرورگر هم معتبر می‌ماند
    | ═══════════════════════════════════════════════════════
    */
    'expire_on_close' => false,

    /*
    | ═══════════════════════════════════════════════════════
    | رمزنگاری نشست — امنیت بیشتر
    | true = محتوای نشست قبل از ذخیره‌سازی رمزنگاری می‌شود
    | ═══════════════════════════════════════════════════════
    */
    'encrypt' => true,

    /*
    | ═══════════════════════════════════════════════════════
    | محل ذخیره‌سازی
    | ═══════════════════════════════════════════════════════
    */
    'files'    => storage_path('framework/sessions'),
    'connection' => env('SESSION_CONNECTION'),
    'table'    => env('SESSION_TABLE', 'sessions'),
    'store'    => env('SESSION_STORE'),

    /*
    | ═══════════════════════════════════════════════════════
    | پاک‌سازی تصادفی نشست‌های قدیمی (Lottery)
    | از هر ۱۰۰ درخواست، ۲ درخواست نشست‌های منقضی را پاک می‌کنند
    | ═══════════════════════════════════════════════════════
    */
    'lottery' => [2, 100],

    /*
    | ═══════════════════════════════════════════════════════
    | کوکی نشست
    | ═══════════════════════════════════════════════════════
    */
    'cookie' => env(
        'SESSION_COOKIE',
        'crm_session'
    ),

    'path'     => env('SESSION_PATH', '/'),
    'domain'   => env('SESSION_DOMAIN'),

    /*
    | ═══════════════════════════════════════════════════════
    | فقط از طریق HTTPS (برای امنیت در production)
    | true = کوکی فقط در اتصال امن ارسال می‌شود
    | ═══════════════════════════════════════════════════════
    */
    'secure' => env('SESSION_SECURE_COOKIE', true),

    /*
    | ═══════════════════════════════════════════════════════
    | دسترسی فقط از طریق HTTP (نه JavaScript)
    | true = جاوااسکریپت نمی‌تواند کوکی نشست را بخواند
    | ═══════════════════════════════════════════════════════
    */
    'http_only' => true,

    /*
    | ═══════════════════════════════════════════════════════
    | Same-Site — جلوگیری از حملات CSRF
    | lax = کوکی فقط در هدایت از همان سایت ارسال می‌شود
    | ═══════════════════════════════════════════════════════
    */
    'same_site' => 'lax',

];